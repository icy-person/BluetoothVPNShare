[package]
name = "bluetooth_vpn_share"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21"
tokio = { version = "1.47", features = ["rt-multi-thread", "net", "io-util", "macros", "sync"] }
url = "2.5"
